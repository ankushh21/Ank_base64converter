from setuptools import setup, find_packages

setup(
    name='ExelaBase64Converter',
    version='0.1.0',
    author='Exela Technologies',
    description='This package converts text pdf to base64 json.',
    # packages=find_packages(),
    install_requires=[
        'ExelaBase64Converter',
    ],
)
